import numpy as np

arr = np.array([1,2,3,4])

# sum
s = np.sum(arr)

# max
maxi = np.max(arr)

# min
mini = np.min(arr)

# count 
c = np.size(arr)

# mean
a = np.mean(arr)

# cumulative frequency (CF)
cf = np.cumsum(arr)

# cumulative product
cp = np.cumprod(arr)
